/**
 * Babel: Convert the React code into a code that the browser understands.
 * Webpack: For each one of the files (.js, .css, .png) it will convert it into a different way.
 *
 * Loaders: babel-loader, css-loader, image-loader
 */
